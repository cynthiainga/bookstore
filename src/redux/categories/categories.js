import React from 'react';

const Categories = () => {
  const styling = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  };

  return (
    <div style={styling}>
      <h2>Under Construction</h2>
    </div>
  );
};

export default Categories;
